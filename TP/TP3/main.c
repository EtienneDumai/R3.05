#include <stdio.h>
#include <unistd.h>
#include <pthread.h>
unsigned long int cpt = 0;
void* compteur(void* arg)
{
    unsigned long int tmp;
    for (int i = 0; i < 1000000; i++)
    {
        
    }
    
}
int main (void)
{
    return 0;
}